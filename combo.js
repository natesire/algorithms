const Permutate = require('./permutations.ts');
const HeapsAlgo = require('./heapsalgo.ts');

var itemsArr: number[]
itemsArr = [1,2,3]

//permutate = new Permutate(itemsArr, PlainsAlgo);
//permutate = new Permutate(itemsArr, );
permutate = new Permutate(itemsArr, HeapsAlgo);

